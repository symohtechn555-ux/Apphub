{
  "name": "appshub",
  "version": "3.0.0",
  "private": true,
  "description": "Appshub — Play-Store style app download site (React + Supabase)",
  "scripts": { "build": "node build.js" },
  "dependencies": { "web-push": "^3.6.7" },
  "devDependencies": { "typescript": "^5.4.0" }
}
